# Analisador de Vídeos com I.A.

Este projeto baixa o áudio de um vídeo do YouTube, transcreve e gera um resumo.

##  Como Rodar
1. Instale as bibliotecas: `pip install -r requirements.txt`
2. Certifique-se de que o `ffmpeg.exe` está nesta pasta.
3. Insira sua chave da OpenAI no arquivo `app.py`.

## 💻 Execução
No terminal, digite:
python app.py "LINK_DO_VIDEO"